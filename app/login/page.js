'use client';
import {useState} from 'react';import {supabase} from '../../lib/supabase';import Brand from '../../components/Brand';
export default function Login(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const send=async(e)=>{e.preventDefault();setMsg('Enviando…');const{error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:location.origin+'/cuenta'}});setMsg(error?error.message:'Revisa tu correo: te enviamos un enlace para entrar.');};return <main className="wrap narrow"><section className="hero loginHero"><Brand/><h1>Bienvenido</h1><p className="sub">Entra o crea tu cuenta con tu correo. No necesitas contraseña.</p><form onSubmit={send} className="stack"><label>Correo electrónico<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="tu@correo.com"/></label><button className="cta">Continuar</button>{msg&&<p className="notice">{msg}</p>}</form><p className="fine">Tus pedidos y datos son privados y sólo aparecen en tu cuenta.</p></section></main>}
